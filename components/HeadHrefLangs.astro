---
interface Props {
  path: string;
}
import { languages } from "../ui.js";
import { generateAlternateLink } from "../utils";
const url = `https://${import.meta.env.PUBLIC_DOMAIN}`;
const { path } = Astro.props;
---

{
  Object.entries(languages).map(([language]) => (
    <link
      rel="alternate"
      hreflang={language}
      href={generateAlternateLink(path, language, url)}
    />
  ))
}
